import { shallowMount } from '@vue/test-utils'
import FlashMessagePanel from '@/components/FlashMessagePanel.vue'

describe('FlashMessagePanel.vue', () => {
  it('does not have any tests yet', () => {
    expect(true).toBe(true);
  });
})
