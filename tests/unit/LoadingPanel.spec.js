import { shallowMount } from '@vue/test-utils'
import LoadingPanel from '@/components/LoadingPanel.vue'

describe('LoadingPanel.vue', () => {
  it('does not have any tests yet', () => {
    expect(true).toBe(true);
  });
})
