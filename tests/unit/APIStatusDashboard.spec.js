import { shallowMount } from '@vue/test-utils'
import APIStatusDashboard from '@/components/APIStatusDashboard.vue'

describe('APIStatusDashboard.vue', () => {
  it('does not have any tests yet', () => {
    expect(true).toBe(true);
  });
})
